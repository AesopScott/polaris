# Codex Instructions

Before starting any task in this repository, read `CLAUDE.md`.

Treat `CLAUDE.md` as mandatory project guidance in addition to this `AGENTS.md` file. Follow its rules for workflow, file safety, versioning, commits, testing, and project-specific conventions.

If `AGENTS.md` and `CLAUDE.md` conflict, follow `CLAUDE.md` unless the user explicitly says otherwise.
